# Title
Q&A回答の展開/折りたたみアニメーション追加

Goal:
- Q&A回答を3行相当で折りたたみ、スムーズに展開できるUIにする

Done:
- `ReviewCard` の回答エリアを `max-h` + `transition-all` でアニメーション対応
- もっと見る/閉じるボタンをテキストスタイルで追加（アイコン回転）
- 折りたたみ時のグラデーションフェードを追加

Open:
- 実データで3行相当の閾値（maxLength）微調整

Notes:
- 文章は切り捨てず、コンテナの高さで視覚的にトリミング
